# stl/: default-parameter STL exports

Optional. Multi-plate PMM models don't offer STL download, so attach these to the listing if
customers need STLs. Name each file exactly like its part (`stl/<part_name>.stl`). Regenerate
them on release when the geometry changed, and delete stale ones.
